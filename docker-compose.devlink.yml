services:
  proxy:
    environment:
      DOMAIN: "${DEVLINK_SITE_ADDRESS:?Defina DEVLINK_SITE_ADDRESS, por ejemplo 203.0.113.10:8443}"
    ports: !override
      - "${DEVLINK_HTTPS_PORT:-8443}:${DEVLINK_HTTPS_PORT:-8443}"
      - "${DEVLINK_HTTPS_PORT:-8443}:${DEVLINK_HTTPS_PORT:-8443}/udp"
